[package]
name = "liminal-horror-game"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.13", features = ["dynamic_linking"] }
rand = "0.8"
noise = "0.8"

[profile.dev]
opt-level = 1

[profile.release]
opt-level = 3
lto = true
